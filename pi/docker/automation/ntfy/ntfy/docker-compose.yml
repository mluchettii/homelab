services:
  ntfy:
    image: binwiederhier/ntfy
    container_name: ntfy
    command:
      - serve
    environment:
      - TZ=America/New_York
    volumes:
      - ./cache/ntfy:/var/cache/ntfy
      - ./config:/etc/ntfy
      - ./db:/var/lib/ntfy
    ports:
      - ${NTFY_HTTP_PORT:-8015}:80
      - ${NTFY_HTTPS_PORT:-9015}:443
    healthcheck:
        test: ["CMD-SHELL", "wget -q --tries=1 http://localhost:80/v1/health -O - | grep -Eo '\"healthy\"\\s*:\\s*true' || exit 1"]
        interval: 60s
        timeout: 10s
        retries: 3
        start_period: 40s
    restart: unless-stopped
    init: true
